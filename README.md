# Mini-Hyper
A minimal Hypervisor used to help the people who are interested in AArch64 hypervisor.

# Steps
1. build uboot for qemu (Only need to run once!):
    sh run_build.sh
2. build hypervisor:
    sh run_build.sh
3. run qemu simulator:
    sh run_qemu.sh
